# Referência TRAMA-TAR Design System

> **Aviso:** Este diretório contém o design system original do **TRAMA-TAR**,
> usado como referência conceitual. O design system **ativo** do RE:COMBO Web
> está definido em [`docs/design-system/DesignSystem.md`](../DesignSystem.md).
>
> Os tokens e regras aqui não refletem necessariamente a implementação atual.
> Consulte sempre o `DesignSystem.md` canônico e os arquivos de configuração
> (`tailwind.config.ts`, `globals.css`) como fonte de verdade.
